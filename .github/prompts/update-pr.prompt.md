---
name: update-pr
description: Update the PR description with the changes from the open PR and set fitting labels.
---

Use tool #openPullRequest and read open PR changes and write pr description and write to temp file and then use `gh` command to insert it into pr description.
Also set fitting label(s) on the PR.
